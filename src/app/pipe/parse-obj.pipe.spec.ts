import { ParseObjPipe } from './parse-obj.pipe';

describe('ParsePageObjPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseObjPipe();
    expect(pipe).toBeTruthy();
  });
});
